CREATE TABLE "event"."event_attachment" ("id" uuid NOT NULL, "event_id" uuid NOT NULL, "attachment_type" text NOT NULL, "title" text NOT NULL, "url" text NOT NULL, "mime_type" text NOT NULL, "description" text NOT NULL, "uploaded_by" uuid NOT NULL, "created_at" timestamptz NOT NULL, PRIMARY KEY ("id") );COMMENT ON TABLE "event"."event_attachment" IS E'Documents and resources attached to events.';
