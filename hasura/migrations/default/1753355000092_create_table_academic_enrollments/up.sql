CREATE TABLE "academic"."enrollments" ("id" serial NOT NULL, "class_id" integer NOT NULL, "student_id" integer NOT NULL, "enrolled_at" timestamptz NOT NULL DEFAULT now(), PRIMARY KEY ("id") , FOREIGN KEY ("class_id") REFERENCES "academic"."classes"("id") ON UPDATE restrict ON DELETE restrict, FOREIGN KEY ("student_id") REFERENCES "academic"."users"("id") ON UPDATE restrict ON DELETE restrict);
