table:
  name: account
  schema: user
array_relationships:
  - name: profiles
    using:
      foreign_key_constraint_on:
        column: user_id
        table:
          name: profile
          schema: user
