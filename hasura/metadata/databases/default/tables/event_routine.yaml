table:
  name: routine
  schema: event
object_relationships:
  - name: course_offering
    using:
      foreign_key_constraint_on: course_offering_id
array_relationships:
  - name: events
    using:
      foreign_key_constraint_on:
        column: routine_id
        table:
          name: event
          schema: event
